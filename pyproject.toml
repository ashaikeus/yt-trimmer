[project]
name = "yt-trimmer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Aleksandra Semenenia", email = "ashaikeus@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "requests>=2.34.2",
    "yt-dlp>=2026.7.4",
]

[project.scripts]
yt-trimmer = "yt_trimmer:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
